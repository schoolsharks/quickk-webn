import { Box, Typography } from "@mui/material";

const MyNetworks = () => {
  return (
    <Box p={2}>
      <Typography variant="h4" mb={2}>
        My Networks
      </Typography>
      <Typography variant="body1">
        Networks content will be displayed here.
      </Typography>
    </Box>
  );
};

export default MyNetworks;