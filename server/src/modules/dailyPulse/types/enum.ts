export enum PulseType {
    InfoCard = "infoCard",
    Question = "question",
    ConnectionFeedback = "connectionFeedback",
}

export enum Status {
    Published = "published",
    Drafts= "drafts",
    Archived= "archived",
}