// Enum for the type field
export enum ModuleType {
    VIDEO = 'video',
    QUESTION = 'question',
}

export enum Status {
    Published = "published",
    Drafts= "drafts",
    Archived= "archived",
}

export enum CompletionStatus{
    INCOMPLETE = "INCOMPLETE",
    COMPLETE = "COMPLETE"
}